services:
  - type: web
    name: sistema-minipa
    runtime: python  # Esta linha é essencial!
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
